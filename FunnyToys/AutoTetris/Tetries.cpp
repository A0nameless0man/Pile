#include "Tetries.h"

namespace Tetries
{
template <size_t M, size_t N, size_t L, typename T>
constexpr T matrixMul(const T (&a)[L][N], const T (&b)[M][L])
{

}

short rotationMatirx[2][2] = { { 0, -1 }, { 1, 0 } };

}  // namespace Tetries